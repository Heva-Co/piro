// Fallback swatch colors for on-call member avatars / Gantt bars when a user has no
// assigned color. Kept in one place so the defaults stay consistent across the feature.
export const DEFAULT_MEMBER_COLOR = "#6366f1"; // indigo-500
export const REPLACED_MEMBER_COLOR = "#94a3b8"; // slate-400
